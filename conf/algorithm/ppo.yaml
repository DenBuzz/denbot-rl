defaults: 
  - base
  - env_config: 1on0
  - env_runners: sweaty

training: 
  gamma: 0.997
  # lr: 1e-4
  train_batch_size_per_learner: 8192
  # num_epochs: 30
  minibatch_size: 1024
  shuffle_batch_per_epoch: True
