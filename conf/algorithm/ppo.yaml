defaults: 
  - base
  - env_config: 1on0
  - env_runners: sweaty

training: 
  gamma: 0.995
  lambda_: 0.95
  lr: 1e-5
  entropy_coeff: 0.005
  train_batch_size_per_learner: 4096
  num_epochs: 32
  minibatch_size: 1024
  shuffle_batch_per_epoch: True
  use_kl_loss: false
